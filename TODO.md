# SSL WebSocket EOF Fix Status
- [x] Replace WS with HTTPClient POST
- [x] Add HTTPS + setInsecure()
- [x] POST_INTERVAL = 30s
- [ ] Test HTTP ngrok tunnel (ngrok http 5000)

**Next**: Run `ngrok http 5000`, update CMS_SERVER_URL to http://newhost.ngrok-free.dev/api/log, upload firmware.

